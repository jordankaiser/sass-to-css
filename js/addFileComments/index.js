/**
 * Add comments to files to indicate where they where created.
 *
 * @param {string} sassFiles - The path to the SCSS files.
 * @return {string} A good string
 *
 * @example
 *
 *     foo('hello')
 */
function addFileComments(sassFiles) {
  console.log('addFileComments');
}

module.exports = addFileComments;